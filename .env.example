# Absen Guru Configuration Template
# Copy this file to .env and customize for your installation

# Database Configuration
DATABASE_URL="file:./absen.db"
DB_FILE_NAME=file:absen.db

# Server Configuration  
PORT=3000
HOST=0.0.0.0

# ⚠️ NODE_ENV Configuration (IMPORTANT)
# NODE_ENV should NOT be set in .env file (not supported by Vite)
# Use command line instead:
#   Development: pnpm dev
#   Network:     ORIGIN=http://192.168.x.x:3000 pnpm start  
#   Production:  NODE_ENV=production ORIGIN=https://domain.com pnpm start

# CSRF Protection - ORIGIN Configuration
# Set ORIGIN environment variable untuk network/production access
# 
# Development (localhost):
#   No ORIGIN needed - auto-detected
#
# Network Testing (IP access):
#   Set via command: ORIGIN=http://192.168.1.100:3000 pnpm start
#   Or use auto-script: ./start-network.sh (Linux/Mac) or start-network.bat (Windows)
#
# Production (domain/VPS):
#   Set via command: NODE_ENV=production ORIGIN=https://yourdomain.com pnpm start
#
# Examples:
# ORIGIN=https://yourdomain.com          # Production domain
# ORIGIN=http://192.168.1.100:3000      # Network IP access
# ORIGIN=http://your-server-ip:3000     # VPS/Cloud IP
# ORIGIN=https://absen-guru.example.com # Subdomain

# Optional: Timezone
TZ=Asia/Jakarta

# Optional: Attendance Rules
ATTENDANCE_CUTOFF_HOUR=8
ATTENDANCE_LATE_MINUTES=15

# Optional: App Branding
APP_NAME="Sistem Absensi Guru"

# DEPLOYMENT NOTES:
# 1. For development: Just run "pnpm dev"
# 2. For network testing: Use auto-scripts or set ORIGIN manually
# 3. For production: Always set NODE_ENV=production and ORIGIN
# 4. Use HTTPS in production for better security
# 5. Configure firewall to allow port 3000